# IMU-SLR-Baseline
IMU-SLR Baselines provides reproducible benchmark implementations for inertial sensor–based sign language recognition. It includes fixed segmentation with ResNet, fixed segmentation with a ViT-style Transformer, and a Seq2Seq model (BiLSTM + GRU + Attention), along with training, evaluation, and configuration scripts for standardized comparison.
